<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.equipealpha.univagas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MeusDadosActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditarDadosPessoaisActivity" />
        <activity android:name=".EditarSenhaActivity" />
        <!-- <activity android:name=".MeusDadosActivity"></activity> -->
        <activity android:name=".EscolherContaActivity" />
        <activity android:name=".CriarContaActivity"></activity>
    </application>

</manifest>